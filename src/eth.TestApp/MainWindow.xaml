<Window x:Class="eth.TestApp.MainWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:eth.TestApp"
             mc:Ignorable="d" 
             Height="600" Width="800" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Send action:" VerticalAlignment="Bottom" Margin="0,0,10,0"/>
            <ComboBox x:Name="ActionComboBox" MinWidth="120" Margin="0,0,10,0"/>
            <Button x:Name="SendChatActionButton" Click="SendChatActionButton_Click">do it</Button>
        </StackPanel>
    </Grid>
</Window>
